name: very_good_coffee
description: which gives the user the ability to save their favorite coffee pictures.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.7.0

dependencies:
  bloc: ^8.1.0
  coffee_api:
    path: packages/coffee_api
  coffee_repository:
    path: packages/coffee_repository
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  local_storage_coffee_api:
    path: packages/local_storage_coffee_api



dev_dependencies:
  bloc_test: ^9.1.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true
